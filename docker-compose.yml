networks:
  global_network:
    external: true

services:
  backend:
    image: nest-v12:latest
    scale: 1
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    expose:
      - "5000"
    networks:
      - global_network

  # proxy:
  #   image: nginx:alpine
  #   container_name: nest-v12-proxy
  #   ports:
  #     - "5000:5000"
  #   volumes:
  #     - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
  #   depends_on:
  #     - backend
  #   networks:
  #     global_network:
  #       aliases:
  #         - nest-v12
